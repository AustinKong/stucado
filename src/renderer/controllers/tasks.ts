// In charge of managing the tasks store and its actions

// Retrieve tasks from backend
const retrieveTasks = async (): Promise<Task[]> => {
}

// Update the store and backend
const updateTasks = async (tasks: Task[]): Promise<void> => {
}